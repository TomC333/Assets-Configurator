export enum CacheTypes {
    IMAGE,
    VIDEO,
    AUDIO,
    JSON,
}

export enum ClickActions {
    CREATE_NEW_PROFILE,
    SWITCH_PROFILE,
    DELETE_PROFILE,
    UPDATE_ASSET,
    UPDATE_ASSET_FROM_LINK,
}